{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "oath:kind:net",
  "title": "net",
  "type": "object",
  "additionalProperties": false,
  "required": ["up", "ipv4"],
  "properties": {
    "up": {
      "type": "boolean",
      "description": "If true, net0 is up. If false, the link is down."
    },
    "ipv4": {
      "type": "string",
      "minLength": 1,
      "description": "IPv4 CIDR (static) or the string dhcp."
    },
    "gateway": {
      "type": "string",
      "description": "IPv4 default gateway. Ignored when ipv4 is dhcp."
    }
  }
}
