# Capabilities — as-built progress

**Purpose:** What exists vs what remains for each product capability.
Target design: [specs/](specs/). Session priority: root [CURRENT.md](../CURRENT.md).
Product docs: [`manual/`](manual/) — current behavior.

**Update this file in the same change** whenever status or gaps change.

**Status vocabulary:** `shipped` · `partial` · `spec’d` · `planned` · `idea`

**As of:** 2026-09-08

---

## Founding band

| ID | Capability | Status | Spec / plan | Dogfood | Gaps | Manual |
|----|------------|--------|-------------|---------|------|--------|
| charter | Principles + independence + AI-first thesis | spec’d | [CURRENT locks](../CURRENT.md) | — | none for v0 | no |
| catalog | Live INDEX + schemas | partial | [freeze](specs/2026-08-27-catalog-and-oath-surface.md) | QEMU probe | INDEX generated; `oath-tel` + `/oath/log` | [catalog](manual/catalog.md) |
| objects | Typed object store | partial | freeze | QEMU | host/svc/snap/pkg/net/ssh/dev | [catalog](manual/catalog.md) |
| oath-cli | `oath` verbs | partial | freeze | QEMU probe | MCP later | [using](manual/using.md) |
| snap | generations; apply / undo | partial | freeze + [T38](specs/2026-09-06-boot-generations.md) | probe (sibling `@gen-N`, reboot); canto `@gen-16` backup send; canto ESP last-5 menu | firmware menu on canto ESP (systemd-boot `BOOTX64` timeout 5; oath-efi logo still paints over it; archives boot 4,7,8,9,10 + current `@`; `oath.subvol=@boot-N`); catalog undo unchanged; off-box send is T33 (partial) | [generations](manual/generations.md) |
| svc | Own supervisor | partial | freeze | QEMU: hold+sshd+seatd+river+sola; canto: sshd + river/Sola as `home` (serial off); pipewire trio seeded | no Requires-style hard deps; canto `svc:serial` off (no UART); live canto pipewire is hand-started as `home` this boot (PID 1 is T38 7.3.0-rc1 #2; ESP initrd has Pitcairn firmware) | [services](manual/services.md) |
| boot | Kernel + init + QEMU | partial | [plan](plans/2026-08-27-qemu-skeleton-plan.md) · [T38](specs/2026-09-06-boot-generations.md) | `cargo make run` / `probe`; canto EFI splash + last-5 menu | vanilla kernel.org + Oath fragment (`image/build-linux.sh`); not Ubuntu generic (that 7.3-rc1 panicked on canto). Layers: `oath-efi` (GOP mark + 5 s boot menu on metal) if EFI; defer KMS that steal firmware fb until River; River black until Sola. ALSA HDA packed and loaded with the KMS pass. Not universal — QEMU `-kernel` has no EFI splash or menu; virtio-gpu is not deferred. Gaps: Apple firmware still first; SI/Pitcairn modeset flash when KMS binds; live canto is compiled vanilla **7.3.0-rc1 #3** with **`DRM_AMD_DC_SI`** (card1 planes advertise GFX6 `IN_FORMATS` + LINEAR); RADV inits after packing Debian **libdrm 2.4.134** into `pkg:mesa`; gamescope still says the Vulkan device has **no DRM format modifiers** (SI nest paint is live RADV LINEAR_ALIGNED on TRANSFER_SRC; tree `0002` is sampled/WSI only); **SI DPM** patches in `image/linux-patches` (DC CRTC timings + RunningOnAC; 5 VBIOS states, PERFORMANCE max 850/1270); live **7.3.0-rc1 #3**; skip GPIO_DC on AC; `force high` **850/1270 MHz**; initrd must pack **Pitcairn firmware** or both GPUs fail `pitcairn_mc.bin`; `gpu_busy_percent` ENOTSUP; Sola menubar GPU graph is NVML-only so it hides on AMD; oath-efi menu still hidden by the logo (systemd-boot is BOOTX64) | [qemu](manual/qemu.md) |
| pkg | Package objects | partial | [freeze](specs/2026-09-05-arcade-steam.md) | QEMU probe + fetchme; canto `pkg:grok` / `pkg:git` / `pkg:curl` / `pkg:pipewire` / `pkg:thoxa` (hand-copied store this boot; thoxa is `c42c9a6` session-rc-split); T35 toolchain live (gen 19) | T20 identity: `pkg.url`, peer Oath host as origin; **T30** `pkg:grok` packed; `pkg:git` + `pkg:curl` + `pkg:pipewire` + `pkg:thoxa` packed; **T35** `pkg:cc` / `pkg:rustc` / `pkg:cmake` / `pkg:pkg-config` packed from official tarballs (`cargo --version` 1.98.1); empty `.pc` farm; **T37** `pkg:bash` / `pkg:xwayland` / `pkg:gamescope` / `pkg:mesa` / `pkg:steam` + `/bin/sola-arcade` on canto (gen 21; steamwebhelper host `_v2-entry-point` + `pkg:mesa` GLX; `oath-lsof` so WebUITransport accepts; direct `steam` is River `+xwayland` session X11 (`DISPLAY=:0`; user confirmed); Arcade Play still nests gamescope (`--nested-steam`; wrapper drops `-b`; unsmoked); Super+Q SIGTERMs X11 class `steam`; launcher Steam; 32-bit RADV loads (`libwayland-client` 1.26 `wl_fixes`; ICD DT_RPATH; GpuTopology PITCAIRN); `liboath-peercred` loads in steamui via dlmopen copy (do not patchelf live `steamui.so`); CEF `--disable-gpu` (GPU SIGBUS 135 on RADV SI); session Steam library paints (X11); SI nest paint: live RADV linear-export TRANSFER_SRC; nested Xwayland `-glamor off` (no SIGBUS); 64-bit liboath-glclass.so stops gldriverquery ELFCLASS32; xdg never-configured was sola-river down (nest holds with WM up; wrappers retry if river is already back and the session is still up; canto gen 35 bounced `svc:sola-river` after Steam killed the compositor); `GAMESCOPE_VIEWPORT_SUPPORTED` faked 0 (value 1 left MainMenu 1×1); RADV SI linear-export for WSI/TRANSFER_SRC; radeonsi keeps INVALID import pitch; do not SET LINEAR_ALIGNED on a real 2D BO (session Steam is an X11 window; Arcade Play nest is `solactl --app` gamescope); 32-bit libgbm in pkg:mesa/lib32; compose.dir aliases C.UTF-8); rootful `:2` leftover `xwayland-clip`/`oath-xwm`; 32-bit RADV `libdisplay-info`+`libxml2`+wayland 1.26; dual ICD jsons; gamescope nest Vulkan + Xwayland in; `libdecor-oath` 1px borders; Steam must not stub `pkg:glibc` `libresolv`); guest cargo of other kit ELFs still **failed** (`alsa-sys` / `alsa.pc`); **T32** pack identity spec’d — not implemented; as-built store is still `/oath/store/pkg/<name>/`; no guest store export, deps, or url-refetch | [packages](manual/packages.md) |
| net | Network objects | partial | [freeze](specs/2026-08-30-ssh-and-dhcp.md) | QEMU probe ping + SSH; canto dhcp 10.0.0.3 | dhcp implemented; wait-for-carrier before rename when several NICs; LAN bridge opt-in (`OATH_BRIDGE`) | [network](manual/network.md) |
| dev | Device objects | partial | [freeze](specs/2026-08-30-libinput.md) | QEMU probe | vda/net0/ttyS0/card0/kbd0/mouse0; no module loader; no udev | [devices](manual/devices.md) |
| ssh | Home SSH via catalog keys | partial | [freeze](specs/2026-09-02-seat-home.md) | QEMU probe login/deny/sudo as `home`; **canto `ssh` / `scp` / `sftp` as `home@canto` (uid 1); login shell `/bin/thoxa`**; sola-terminal / workspaces `$SHELL` thoxa (wrappers this boot) | dropbear `-w`; uid 1; sudo ALL no password; groups `root`+`home`; interactive PTY (`ptmxmode=0666`); no baked private key; `pkg:dropbear` includes musl OpenSSH `/bin/ssh` + `/bin/scp` + `/bin/sftp-server` (canto live; next `cargo make build` for QEMU). Guest client `accept-new` host keys. Editor is busybox `/bin/vi`. Home login shell is `/bin/thoxa` (must be in `/etc/shells` or dropbear looks like publickey denied). pkg:sola wrappers must not force `SHELL=/bin/sh`. Do not write `$HOME/.profile` | [ssh](manual/ssh.md) |
| agent | Agent runtime as a system component | idea | — | — | serial or SSH client | no |
| update | Base-image updates | idea | — | — | generations are the primitive; off-box copy is T33 | no |
| backup | Off-box generation (NFS) | partial | [freeze](specs/2026-09-03-backup-nfs.md) | canto gen 16 → nas `alpha/backup/canto`; daily 04:00 Mountain sleeper | T33: one overwrite send; `backup-daily` 04:00 Mountain; crash-consistent + optional pack quiesce/thaw. Gaps: live this boot nfs.ko was insmod’d; T38 ESP initrd packs nfs (after reboot); QEMU next image; no restore; no packed quiesce hooks yet | [services](manual/services.md) |
| install | Installer to a disk | partial | [freeze](specs/2026-08-31-metal-canto.md) · [T38](specs/2026-09-06-boot-generations.md) | QEMU-EFI rehearsal + canto T31 reinstall + canto `esp` rotate | `cargo make install --target --disk --confirm`; `--usb` EFI ramdisk; `cargo make esp --esp --confirm` rotates last 5 without wipe (works without qemu/nix when `OATH_KERNEL`/`OATH_MODULES`/`OATH_BUSYBOX` are set). `oath-efi` reads `loader.conf` + `oath-boots`. Post-install SSH is `home`. ESP initrd `/init` stays PID 1 after chroot (`oath.subvol=`). Gaps: kexec not used on this Apple; metal timeout menu is systemd-boot (oath-efi logo still hides the picker) | [install](manual/install.md) |
| sola | Sola session on Oath | partial | [freeze](specs/2026-09-02-seat-home.md) | QEMU session probe (stack as `home` uid 1); canto T31 SSH + River/Sola as `home` (uid 1, amdgpu DP-10, gles2/radeonsi); oath-sola packed live (LED graphs rastered); volume chip + Built-in Audio this boot; **sola-kvm listen** on canto (UDP 4242, virtual pointer; CLIP1 text + PNG; Super+Tab confirm is key-before-modifiers; kernel auto-repeat not injected, libexec hand-copied this boot); sola-terminal / workspaces `$SHELL` `/bin/thoxa` (wrappers this boot, not ash; wrappers `cd $HOME` so a new Terminal is not `/`); tmux clients work (Steam must not stub `pkg:glibc` `libresolv`); **T34** menubar clock Mountain via `host:local.timezone` (`date` UTC) | T31 QEMU + canto metal: graphical stack as `home`; kit ELFs in `pkg:sola` packed (flower Restart/Shutdown via `oath apply --confirm`; Super+Tab counts, notify pile, spectrum, rounded floats, browser omnibox/devtools; **`/bin/sola-kvm`**); virtio-only SW cursor + pixman + `LIBGL_ALWAYS_SOFTWARE`; metal: gles2/radeonsi; `pkg:grok` / `pkg:git` / `pkg:curl` / `pkg:pipewire` / `pkg:thoxa` packed; **T35** toolchain live (gen 19); **T36** remaining kit packing list in; **T37** `/bin/sola-arcade` + Steam/gamescope/Xwayland/`pkg:mesa` on canto (steamwebhelper host `_v2-entry-point`; CEF GPU `BrowserReady`; WebUITransport accepts via `oath-lsof`; direct `steam` is River `+xwayland` session X11 (`DISPLAY=:0`; user confirmed); Arcade Play still nests gamescope (`--nested-steam`; wrapper drops `-b`; unsmoked); Super+Q SIGTERMs X11 class `steam`; launcher Steam; 32-bit RADV loads (`wl_fixes` wayland 1.26; GpuTopology PITCAIRN); `liboath-peercred` loads in steamui via dlmopen copy (do not patchelf live `steamui.so`); CEF `--disable-gpu` (GPU SIGBUS 135 on RADV SI); session Steam library paints (X11); SI nest paint: live RADV linear-export TRANSFER_SRC; nested Xwayland `-glamor off` (no SIGBUS); 64-bit liboath-glclass.so stops gldriverquery ELFCLASS32; xdg never-configured was sola-river down (nest holds with WM up; wrappers retry if river is already back and the session is still up; canto gen 35 bounced `svc:sola-river` after Steam killed the compositor); `GAMESCOPE_VIEWPORT_SUPPORTED` faked 0 (value 1 left MainMenu 1×1); RADV SI linear-export for WSI/TRANSFER_SRC; radeonsi keeps INVALID import pitch; do not SET LINEAR_ALIGNED on a real 2D BO (session Steam is an X11 window; Arcade Play nest is `solactl --app` gamescope); 32-bit libgbm in pkg:mesa/lib32; compose.dir aliases C.UTF-8); rootful `:2` leftover `xwayland-clip`/`oath-xwm`; 32-bit RADV + dual ICD; gamescope nest Vulkan + Xwayland in (`libdecor-oath` 1px borders); Arcade Play unsmoked (gamescope wrapper drops `-b`; quit session Steam first); `CLONE_NEWUSER` EPERM from PID 1 chroot); guest cargo failed on `alsa.pc` so settings/mail/spotify/wrapper/… still not in `/bin`; no dbus-daemon; `/bin/xdg-open` is `solactl open` (not xdg-utils / portal); PipeWire HDMI not udev-enumerated (PCH analog pinned); live pipewire + sola-kvm daemons are seat processes this boot, not yet ESP-initrd svcs; kvm Super+Tab confirm unsmoked after inject-order fix | [services](manual/services.md) |
